# 여기서도 md 파일 작성이 가능
근데 md 파일은 vscode에서 작성

그러면 js 프로젝트에서 git push 해놓겠음.

# learning_logs 기준 vscode에서 열기
당분간 .md 파일 작성은 vscode에서, python code는 pycharm에서 작성
여기서 쓰는게 pycharm에서 반영되는지 확인

pycharm에서 github repository로 올릴 수 있도록 설정
git init
git config user.name "깃허브아이디"
git config user.email "깃허브이메일"
git config user.name
git config user.email